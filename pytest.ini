[pytest]
markers =
    smoke: marks tests as smoke tests
    regression: marks tests as regression tests

python_files = test_*.py *Login.py CalculatorApp.py

# Playwright settings
addopts =
    --browser chromium
    --browser-channel chrome
    --headed
    --alluredir=allure-results
    --clean-alluredir